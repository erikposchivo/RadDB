"""Command-line maintenance utilities for RadDB archives.

These are one-shot operational scripts, not part of the library API — nothing
in :mod:`raddb` imports them.  Run them with ``python -m raddb.tools.<name>``.
"""
